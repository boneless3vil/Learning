# photocat — photo catalog & search
# Core (required for: init, scan, dedup, search by metadata)
Pillow>=10.2.0          # image loading + EXIF
piexif>=1.1.3           # robust EXIF read (GPS, dates)
ImageHash>=4.3.1        # perceptual hashing for near-duplicate detection
reverse_geocoder>=1.5.1 # OFFLINE GPS -> place name (no network)
numpy>=1.26
rich>=13.7              # pretty CLI output + progress
typer>=0.12             # CLI framework

# AI enrichment (required only for: enrich). Big downloads.
# Install the CUDA build of torch for your GPU FIRST, see README.
torch>=2.2
torchvision>=0.17
open_clip_torch>=2.24   # semantic image embeddings (natural-language search)
transformers==4.44.2    # Florence-2 needs this exact line; newer breaks its remote code
einops>=0.7             # required by Florence-2
timm>=0.9               # required by Florence-2

# Optional: faces -> names (only if you enable faces in config)
# insightface>=0.7.3
# onnxruntime-gpu>=1.17
